providers:
  - anthropic:messages:claude-sonnet-4-5-20250929

prompts:
  - file://../app/services/prompts/templates/chat_global.md

tests:
  # Global Chat Test
  - description: "Global chat - User having rough day"
    vars:
      companion_name: "Sol"
      personality_descriptor: "warm and empathetic"
      traits_list: "Friendly, Supportive"
    assert:
      - type: llm-rubric
        value: "Response is warm, empathetic, and creates safe space for conversation"
      - type: llm-rubric
        value: "Response acknowledges difficulty and offers support"
